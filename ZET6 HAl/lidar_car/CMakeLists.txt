# -----------------------------------------------------------------------------
# @author    Auto-generated by STM32CubeIDE for Visual Studio Code Extension
# -----------------------------------------------------------------------------
# @attention
#
# Copyright (c) 2025 STMicroelectronics.
# All rights reserved.
#
# This software is licensed under terms that can be found in the LICENSE file
# in the root directory of this software component.
# If no LICENSE file comes with this software, it is provided AS-IS.
#
# -----------------------------------------------------------------------------
 
 cmake_minimum_required(VERSION 3.20)

#
# This file is generated only once, and is not re-generated.
#
# User is free to modify the file as much as necessary
#

# Core project settings
project("lidar_car")
enable_language(C CXX ASM)
message("Build type: " ${CMAKE_BUILD_TYPE})

# Setup compiler settings
set(CMAKE_C_STANDARD 11)
set(CMAKE_C_STANDARD_REQUIRED ON)
set(CMAKE_C_EXTENSIONS ON)
set(CMAKE_CXX_STANDARD 20)
set(CMAKE_CXX_STANDARD_REQUIRED ON)
set(CMAKE_CXX_EXTENSIONS ON)

# Core MCU flags, CPU type, instruction set and FPU setup
set(cpu_PARAMS 
    # Other parameters
    # -mthumb
    # -mcpu, -mfloat, -mfloat-abi, ...
)

# Select one application entry point per build directory. The default remains
# Src/main.c so parallel Nano/servo work is not overwritten by test presets.
set(LIDAR_CAR_MAIN_SOURCE
    "${CMAKE_CURRENT_SOURCE_DIR}/Src/main.c"
    CACHE FILEPATH "Application main source for this build")

# Optional actuator gate used by the Bluetooth motor test application.
set(BLUETOOTH_MOTOR_OUTPUT_ENABLE_VALUE
    "0"
    CACHE STRING "0=preview only, 1=enable motor PWM output")

set(BLUETOOTH_BAUDRATE_VALUE
    "9600"
    CACHE STRING "USART3 Bluetooth baud rate")

set(GYRO_BAUDRATE_VALUE
    "9600"
    CACHE STRING "USART2-remapped PD5/PD6 WIT gyro baud rate")

# Sources
set(sources_SRCS
	${LIDAR_CAR_MAIN_SOURCE}
	${CMAKE_CURRENT_SOURCE_DIR}/Start/system_stm32f10x.c
	${CMAKE_CURRENT_SOURCE_DIR}/System/Delay.c
	${CMAKE_CURRENT_SOURCE_DIR}/Library/stm32f10x_rcc.c
	${CMAKE_CURRENT_SOURCE_DIR}/Library/stm32f10x_gpio.c
	${CMAKE_CURRENT_SOURCE_DIR}/Library/stm32f10x_tim.c
	${CMAKE_CURRENT_SOURCE_DIR}/Library/stm32f10x_adc.c
	${CMAKE_CURRENT_SOURCE_DIR}/Library/stm32f10x_usart.c
	${CMAKE_CURRENT_SOURCE_DIR}/Hardware/bsp_l298n.c
	${CMAKE_CURRENT_SOURCE_DIR}/Hardware/bsp_motor.c
	${CMAKE_CURRENT_SOURCE_DIR}/Hardware/bsp_aux_tb6612.c
	${CMAKE_CURRENT_SOURCE_DIR}/Hardware/app_four_wheel_tb6612_test.c
	${CMAKE_CURRENT_SOURCE_DIR}/Hardware/bsp_diag_uart.c
	${CMAKE_CURRENT_SOURCE_DIR}/Hardware/bsp_robot_uart.c
	${CMAKE_CURRENT_SOURCE_DIR}/Hardware/bsp_gyro_wit.c
	${CMAKE_CURRENT_SOURCE_DIR}/Hardware/bsp_gray_tracking.c
	${CMAKE_CURRENT_SOURCE_DIR}/Hardware/bsp_motor_safe.c
	${CMAKE_CURRENT_SOURCE_DIR}/Hardware/bsp_mecanum.c
	${CMAKE_CURRENT_SOURCE_DIR}/Hardware/app_ladrc.c
	${CMAKE_CURRENT_SOURCE_DIR}/Hardware/app_servo_test.c
	${CMAKE_CURRENT_SOURCE_DIR}/Hardware/app_bluetooth_motor_test.c
	${CMAKE_CURRENT_SOURCE_DIR}/Hardware/app_nano_uart_test.c
	${CMAKE_CURRENT_SOURCE_DIR}/Hardware/nano_uart_diag_log.c
	${CMAKE_CURRENT_SOURCE_DIR}/Hardware/motor_diag_log.c
	${CMAKE_CURRENT_SOURCE_DIR}/Hardware/motor_tb6612_test.c
	${CMAKE_CURRENT_SOURCE_DIR}/Hardware/app_gray_tracking_test.c
)

# Include directories for all compilers
set(include_DIRS
	${CMAKE_CURRENT_SOURCE_DIR}/Inc
	${CMAKE_CURRENT_SOURCE_DIR}/Start
	${CMAKE_CURRENT_SOURCE_DIR}/Library
	${CMAKE_CURRENT_SOURCE_DIR}/System
	${CMAKE_CURRENT_SOURCE_DIR}/Hardware
)

# Include directories for each compiler
set(include_c_DIRS)
set(include_cxx_DIRS)
set(include_asm_DIRS)

# Symbols definition for all compilers
set(symbols_SYMB
	STM32F10X_HD
	USE_STDPERIPH_DRIVER
)

# Symbols definition for each compiler
set(symbols_c_SYMB)
set(symbols_cxx_SYMB)
set(symbols_asm_SYMB)

# Link directories and names of libraries
set(link_DIRS)
set(link_LIBS)

# Linker script
set(linker_script_SRC)

# Compiler options
set(compiler_OPTS)

# Linker options
set(linker_OPTS)

# Now call generated cmake
# This will add script generated
# information to the project
include("cmake/vscode_generated.cmake")

# Link directories setup
# Must be before executable is added
link_directories(${CMAKE_PROJECT_NAME} ${link_DIRS})

# Create an executable object type
add_executable(${CMAKE_PROJECT_NAME})

# Add sources to executable
target_sources(${CMAKE_PROJECT_NAME} PUBLIC ${sources_SRCS})

# Add include paths
target_include_directories(${CMAKE_PROJECT_NAME} PRIVATE
    ${include_DIRS}
    $<$<COMPILE_LANGUAGE:C>: ${include_c_DIRS}>
    $<$<COMPILE_LANGUAGE:CXX>: ${include_cxx_DIRS}>
    $<$<COMPILE_LANGUAGE:ASM>: ${include_asm_DIRS}>
)

# Add project symbols (macros)
target_compile_definitions(${CMAKE_PROJECT_NAME} PRIVATE
	${symbols_SYMB}
	BLUETOOTH_MOTOR_OUTPUT_ENABLE=${BLUETOOTH_MOTOR_OUTPUT_ENABLE_VALUE}
	BLUETOOTH_BAUDRATE=${BLUETOOTH_BAUDRATE_VALUE}
	GYRO_BAUDRATE=${GYRO_BAUDRATE_VALUE}
    $<$<COMPILE_LANGUAGE:C>: ${symbols_c_SYMB}>
    $<$<COMPILE_LANGUAGE:CXX>: ${symbols_cxx_SYMB}>
    $<$<COMPILE_LANGUAGE:ASM>: ${symbols_asm_SYMB}>

    # Configuration specific
    $<$<CONFIG:Debug>:DEBUG>
    $<$<CONFIG:Release>: >
)

# Add linked libraries
target_link_libraries(${CMAKE_PROJECT_NAME} ${link_LIBS})

# Compiler options
target_compile_options(${CMAKE_PROJECT_NAME} PRIVATE
    ${cpu_PARAMS}
    ${compiler_OPTS}
    -fstack-usage
    # -fcyclomatic-complexity
    -Wall
    -Wextra
    -Wpedantic
    -Wno-unused-parameter
    $<$<COMPILE_LANGUAGE:C>: >
    $<$<COMPILE_LANGUAGE:CXX>:

    # -Wno-volatile
    # -Wold-style-cast
    # -Wuseless-cast
    # -Wsuggest-override
    >
    $<$<COMPILE_LANGUAGE:ASM>:-x assembler-with-cpp -MMD -MP>
    $<$<CONFIG:Debug>:-O0 -g3 -ggdb>
    $<$<CONFIG:Release>:-Os>
)

# Linker options
target_link_options(${CMAKE_PROJECT_NAME} PRIVATE
    -T${linker_script_SRC}
    ${cpu_PARAMS}
    ${linker_OPTS}
    -Wl,-Map=${CMAKE_PROJECT_NAME}.map
    -u _printf_float # STDIO float formatting support (remove if not used)
    --specs=nosys.specs
    -Wl,--start-group
    -lc
    -lm
    -lstdc++
    -lsupc++
    -Wl,--end-group
    -Wl,-z,max-page-size=8 # Allow good software remapping across address space (with proper GCC section making)
    -Wl,--print-memory-usage
)

# Conditionally add CMSE linker options
if(CMSIS_Dsecure STREQUAL "Secure")
    target_link_options(${CMAKE_PROJECT_NAME} PRIVATE
        -Wl,--cmse-implib
        -Wl,--out-implib=secure_nsclib.o
    )
endif()

# Set linker script as a dependency to force re-link if it changes
set_target_properties(${CMAKE_PROJECT_NAME} PROPERTIES LINK_DEPENDS ${linker_script_SRC})

# Execute post-build to print size, generate hex and bin
add_custom_command(TARGET ${CMAKE_PROJECT_NAME} POST_BUILD
    COMMAND ${CMAKE_SIZE} $<TARGET_FILE:${CMAKE_PROJECT_NAME}>
    COMMAND ${CMAKE_OBJCOPY} -O ihex $<TARGET_FILE:${CMAKE_PROJECT_NAME}> ${CMAKE_PROJECT_NAME}.hex
    COMMAND ${CMAKE_OBJCOPY} -O binary $<TARGET_FILE:${CMAKE_PROJECT_NAME}> ${CMAKE_PROJECT_NAME}.bin
)
