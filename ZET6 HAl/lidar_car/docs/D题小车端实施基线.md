# D 题小车端实施基线

## 1. 目标与范围

本文件定义 D 题陆空协同系统的小车端运行态。小车以八路灰度循迹为主路径依据，编码器闭环负责速度，陀螺仪只做航向辅助，雷达/树莓派提供平台中心坐标给 LoRa 协同。小车不负责无人机的起飞、投放、着陆判定或飞控状态机。

通信唯一依据为 `F:\keil5\stm\ZET6 HAl\lidar_car\docs\D题_通用通信与接口规范_v2.3.docx`；
在线帧 Version 仍为 `0x02`。
角色实现约束见 `F:\keil5\stm\docs\D题_Agent修改提示词_v2.2.md`。题目澄清见
[D题 回复.txt](D题%20回复.txt)。

## 2. 当前硬件事实

| 子系统 | 已确认事实 | 状态 |
| --- | --- | --- |
| 前轮 TB6612 | PWM `PA2/PA3`，方向 `PE2-PE5`，STBY `PE6` | 当前运动基线 |
| 前轮编码器 | 左 `PA0/PA1 TIM5`，右 `PA6/PA7 TIM3` | 当前反馈基线 |
| 八路灰度循迹 | 地址线 `AD0/AD1/AD2=PC0/PC1/PC2`，数字 `OUT=PG0` | Yahboom 74HC4051 地址扫描接口；X1--X8 已实机验证为 `0x01`--`0x80`，白底基准 `0x00`；X4 左偏 / X4+X5 居中 / X5 右偏的误差符号已验证 |
| 后轮 TB6612 测试 | PWM `PE13/PE14`，方向 `PF1-PF4`，STBY `PB9` | 仅架空四轮测试；与灰度主组无已知引脚冲突 |
| 后轮编码器 | 左 `PB6/PB7`，右 `PC6/PC7` | 目前仅输入轮询，TIM4/TIM8 闭环待完成 |

后轮 STBY 已改接 PB9，灰度主组 `PC0/PC1/PC2 + PG0` 不再与后轮驱动冲突。`FourWheelTb6612Debug` 仍只用于电机带起，因为它不初始化灰度解码、循迹控制和任务状态机；比赛固件需在四轮回归通过后另行验证。

## 3. 灰度循迹与速度策略

1. 已在实际白底黑线地图完成八头验证：`all_white_mask=0x00`，X1--X8 的激活位依次为 `0x01/0x02/0x04/0x08/0x10/0x20/0x40/0x80`，证据为 `logs/gray_mux_retest_20260730-104601.log`。正常黑线居中时 X4+X5 稳定为 `center_mask=0x18`，证据为 `logs/gray_mux_center_x4_x5_20260730-110625.log`；车体左/右偏时分别稳定为 X4=`0x08`、X5=`0x10`，证据为 `logs/gray_mux_vehicle_left_x4_20260730-111544.log` 与 `logs/gray_mux_vehicle_right_x5_20260730-111651.log`。误差诊断镜像重标定后的三点回归已通过：中心 `0`、左 `-100`、右 `+100`，证据为 `logs/gray_error_diag_*_20260730-112*.log`。诊断权重 `-7/-5/-3/-1/+1/+3/+5/+7` 的负值表示黑线在车体左、正值表示车体右。`LineFollowJY901Debug` 将误差转换为非线性灰度差速参考，再由直连 WIT/JY901 yaw 和由角度帧导出的 yaw-rate 产生有界航向补偿，并交给前轮双 `SpeedLadrc_t` 速度内环；前轮输出同时驱动后轮同侧的**开环跟随**。物理前进符号以四驱合格记录为准：前左/前右/后左/后右=`-1/-1/+1/-1`，并由 `bsp_four_wheel_direction.h` 同时供四驱测试与循迹镜像使用。后轮 TIM4/TIM8 编码器未标定，不能称为四轮速度闭环；基础目标为 600 mm/s（3520 counts/s 暂定换算）。这是已构建的首跑方案，不是落地循迹验收；PG10 实体按键、编码器/gyro 活性门、丢线 650 ms 超时和冻结日志必须共同实测。UART4 雷达位姿仅保留为后备解析接口，当前不与 JY901 航向源混用。旧 ADC 采样是错误驱动，不能作为当前映射结论。
2. A 点起始全黑区域已实测为稳定 `active_mask=0xFF`，证据为 `logs/gray_mux_all_black_A_20260730-110324.log`。`LineFollowMissionDebug` 要求该签名和 PG10 实体按键共同启动；起跑后以受限 400 mm/s 离开 A，回到 A 持续 200 ms 才判定一圈完成并停车。
3. 灰度阵列给出主横向误差和 yaw-rate 参考；速度内环消费编码器反馈；陀螺仪 yaw/yaw-rate 只做有界差速修正，不替代灰度循迹。雷达位置仍不加入本轮控制闭环。
4. `LineFollowJY901Debug` 已在 2026-07-30 完成实车连续一圈及自动终点停车：原基线为 600 mm/s
   基础档、280 mm/s 紧急弯道和 2200 cps 外侧差速，JY901 在冻结
   窗口内保持新鲜。首个成功一圈证据为
   `logs/line_follow_frozen_20260730-170644.log`；自动停车复验为
   `logs/line_follow_frozen_20260730-190434.log`，终点原因
   `A_RETURN_AFTER_2_ARCS`，说明 B--C 与 D--A 两个同向半圆已由累计 JY901
   偏航阶段区分。详见 `logs/line_follow_terminal_acceptance_20260730.md`。
   该次有 71.7% 冻结样本处于 `|gray_err_x100|>=400` 的外侧传感器区。当前镜像以
   不降低正常弯道基础速度为原则，将差速上限提升至 5000 cps、增强 X4/X5 中心捕获；外侧
   误差达到 400 时仅给外轮增加 10% PWM，并在边缘→失线时保留边缘方向、否则使用最近
   三帧差速寻线；该改动已构建，待下一次落地验收。
5. A 到 B 为协同段。小车开始运动后计时，必须在 15 s 内到达 B。通过实际场地重复测量选择带余量的速度，不把“慢一点”作为无上限的策略。
6. B 后直线可采用独立的较高速度；当前普通弯道保持 600 mm/s 基础速度，以差速而非降速纠偏。
   丢线恢复和载荷不稳处可使用单独的受限速度。每个速度档都要记录速度、循迹误差、越线/丢线次数和停车距离。
7. 丢线时优先保持最近 180 ms 的边缘方向；没有有效边缘时以最近三帧差速均值搜索，650 ms 未恢复即停车；无线 ACK、地面站显示和飞行状态都不应成为小车停车依据。

## 4. LoRa 与协同边界

树莓派将雷达/SLAM 坐标换算为车载平台中心的 `FIELD_GLOBAL` 位姿，并以 115200 8N1、10 Hz 发送给 MCU。MCU 校验后成为唯一 LoRa 发射者：

- 广播 `CAR_POSE`（0x80）10 Hz，坐标单位 cm、yaw 单位 0.1 度；按地面站联调决定，
  MCU 转发到 LoRa 时仅对 `X/Y` 取反，`yaw` 与速度字段保持 Pi/radar 原值；
- 每个 100 ms 周期仅在 0-25 ms 的车端槽发射；30-55 ms 留给机端回应，55-100 ms 静默；
- 车上物理按键发起抛投或动态起降请求，固定任务 ID/序列最多重发三次；
- 只有 Pi 成功应用校准后才确认校准；任务中不得更改 `CalibrationId`；
- 当前物理维护事件为 PG9 低有效长按 2 s，且 `CAR_RUNNING=0` 连续 12 s；按当前联调
  决定 MCU 不向雷达/Pi 写入，直接在连续三个车端时隙广播同一 ResetId/Seq/Payload 的
  `MAINTENANCE_RESET(0x85)`；此配置只在 Pi 已未校准时用于地面站联调。若 Pi 以后保留
  有效校准，必须恢复 V2.3 C.3 的 Pi 清除/ACK 事务，不能用此事件代替真实校准清除；
- 任务中小车持续循线一整圈，不因 LoRa ACK、遥测延迟或地面站状态而重复启动或停车。

LoRa 透明无线模块已于 2026-07-30 完成独立双向台架验证：MCU 采用 `UART5`、115200 8N1，
`PC12=MCU TX -> 无线模块 RX`，`PD2=MCU RX <- 无线模块 TX`。PC 侧远端模块为 COM12，
接收到了逐字节一致的 33 B `CAR_POSE` 官方测试向量；反向发送 23 B `CAL` 官方测试向量后，
MCU 记录了 CRC 有效且字段匹配的 `rx_ok`。该结论仅证明无线传输层，尚未启用 10 Hz 位姿、
时隙、任务重传、ACK 或校准转发。UART5 不得再同时接 Pi/Nano；Pi 位姿入口需另选并验证接口。

当前 `LineFollowJY901Debug` 在没有新鲜 Pi 位姿且小车静止时每 500 ms 发送 `HEARTBEAT(0x03)`；它采用
固定 8 B 载荷 `DeviceStatus(u8) + ErrorCode(u8) + Reserved(u16=0) + UptimeMs(u32, big-endian)`。
一旦 UART4 位姿新鲜，心跳让位给 10 Hz `CAR_POSE(0x80)`；心跳不能替代其 22 B 载荷、车端时隙和校准门禁。

2026-07-30 已由地面站 `raspi@192.168.0.133` 的 `/dev/ttyUSB0` 实测接收：8 B 修复镜像运行后，
`valid` 计数按约 2 Hz 增长，而 CRC、长度和 payload 丢弃计数不再增长。此项仅为维护心跳
台架验收，`car_age=-1` 在未发送 `CAR_POSE` 时属预期结果；证据见
`logs/line_follow_radio_heartbeat_groundstation_20260730.md`。

V2.3 位姿转发代码现已接入同一镜像：UART4 首选 Pi 的
`CAR_POSE(src=0x31,dst=0x32,len=22)`，校验 CRC、位置/yaw 有效位、yaw 范围、速度无效哨兵值和
`CALIBRATED=>CalibrationId!=0` 后，经 UART5 以 `src=0x30,dst=0x10` 每 100 ms 广播。为兼容当前
物理 Pi 的 14 B `FA...AB` 本地桥接帧，MCU 也可将其封装为未校准的预检 `CAR_POSE`；该兼容路径
不置 `CALIBRATED`、从首个有效旧帧启动 MCU 转发 epoch 作为临时 `SourceTimeMs`，不能用于正式任务。MCU 依据真实循迹状态改写
`CAR_RUNNING` 位；Pi 数据超过 250 ms 后，车辆跑动中保持 LoRa 静默，静止时退回维护心跳。

2026-07-30 的后续回归中，UART4 RXNE 环形缓冲消除了前台轮询造成的帧失步；MCU 已从 14 B 兼容流向地面站
持续产生新鲜 `CAR_POSE`（`car_age<=0.098s`，约 10 Hz，UART4 原始输入约 20 Hz）。静止原始帧解码为
`X=6 cm,Y=8 cm,yaw=9.6°`，说明 Pi 雷达零位仅有数厘米残差而非 MCU 载荷字节序错误；详见
`logs/car_pose_legacy_bridge_groundstation_20260730.md`。该记录只证明未校准显示链路，不解除 Pi V2.2、
校准、时隙、维护复位与任务验收要求。当前实装 Pi 仍仅输出 `FA...AB` 旧桥接帧；按当前
地面站联调决策 PG9 不再向 Pi 请求 ACK，而是直接发送三帧 `0x85`。该例外不清除 Pi 状态，
不得在 Pi 保存了有效 CalibrationId 时用于正式复位。

## 5. MaixCam V2.3 协同边界

MaixCam Pro 只与飞控进行 `CAMERA_MODE`、`CAMERA_TARGET`、`CAMERA_ACTION` 和
`CAMERA_ACTION_RESULT` 会话。飞控在接受小车任务后创建 `ModeSeq`，并以
`TaskType + MissionId + ModeSeq` 约束视觉目标和投放动作。小车 MCU 与树莓派：

- 只产生并保持 `CAR_TASK_REQUEST` 的 `TaskType/MissionId`，持续发送经 Pi
  校准的 `CAR_POSE`；
- 不生成或解析 `ModeSeq`，不解析相机 TARGET/RESULT，不直接驱动相机或投放舵机；
- 不因相机模式 ACK、视觉目标丢失、投放 RESULT 或遥测延迟停止、重启或改变循迹。

雷达/Pi 位姿仍是空地协同的主闭环输入；MaixCam 仅在飞控已建立有效会话后的末段
提供机体系纠偏。

## 6. 运行态与验收

```text
IDLE -> 起飞前校准 -> 车上按键选任务 -> A-B 灰度循迹/位姿10Hz
     -> 其余赛道循迹/伴飞 -> 完成或安全停止
```

最小验收证据：

| 项目 | 必须记录 |
| --- | --- |
| 灰度阵列 | X1--X8 地址扫描位号、`all_white_mask`、地图白黑极性、误差符号、丢线策略；记录每头 `CHANNEL_SWITCH` 日志 |
| A-B | 场地版本、至少多次用时、选定速度和 15 s 余量 |
| 电机 | 指令方向、实际方向、编码器符号、最大安全 PWM |
| LoRa | 帧类型、序列、CRC、位姿年龄、时隙时刻、重发次数 |
| 任务按钮 | 任务类型、任务 ID、单次触发和去重结果 |
| 空地会话边界 | 小车任务 ID 与飞控/相机状态匹配记录；无小车直接相机控制 |

地面站只显示场地、小车、无人机、链路新鲜度和校准/任务状态。任务运行期间不应提供小车启动、停止、调参或代码下发控制。
