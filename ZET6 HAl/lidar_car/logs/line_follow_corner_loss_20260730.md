# 无陀螺仪循迹：首个转弯丢线记录（2026-07-30）

- 镜像：`LineFollowNoGyroDebug`，PG10 从全黑 A 点启动。
- 结果：直线离开 A 点并进入循迹；第一个转弯后白底丢线，安全停止。
- 冻结证据：`line_follow_frozen_20260730-145950.log`，终止原因为
  `LINE_LOST_TIMEOUT`。
- 关键序列：传感器黑线由 X5（`0x10`）向 X6/X7/X8（最高 `0x80`）移动；
  最大灰度误差为 700。旧差速上限仅为 700 cps，丢线后又因当前灰度误差清零而
  以零差速盲行。
- 速度环证据：TRACK 段左右目标/实测平均总误差约 186 cps，非本次首要故障。
- 后续修正：外侧探头非线性差速上限提升至 1600 cps；无陀螺仪急弯降至
  220 mm/s；丢线时保留最后有效转向并以 180 mm/s 最多找线 650 ms。
- 验收待办：同一 A 点和同一首弯复测；必须导出新的冻结日志后才确认修正有效。

## 直连 JY901 首弯复测（2026-07-30 16:56）

- 镜像：`LineFollowJY901Debug`，冻结证据：
  `line_follow_frozen_20260730-165635.log`。
- 终止原因：`LINE_LOST_TIMEOUT`；JY901 全程 `gyro_fresh=1`，故不是陀螺仪
  或编码器失活造成的停车。
- 传感器序列从 `X5/X6=0x30` 逐步移向 `X7=0x40`、`X8=0x80` 后成为
  `0x00`，表明黑线从车体右侧逃出探头范围。
- 控制器已给出右转命令：急弯时 `total_diff=-1600 cps`，左/右目标约为
  `3264/1664 cps`，JY901 yaw 由 `1066` 降至 `687`（约右转 38°），但在
  当前 420 mm/s 急弯速度下仍未及时切入弯道。
- 下一轮唯一调节：保留差速和方向符号不变；当 `abs(error)>=500`（X7/X8）
  时，将切换到 280 mm/s 紧急弯道速度，再复测同一首弯。

## 日志判定与第二轮路径级调节

- `0x7e`（X2--X7）被识别为 `WIDE` 时，旧逻辑把目标从约 3520 cps 降至
  约 2230 cps；该宽线是赛道 B 点，不是停车/减速标记，故本轮取消此默认
  降速，宽线保持当前基础速度。
- 在 X7/X8，左右轮实测已经接近各自目标（例如 `3264/1664 cps` 对
  `3200/1775 cps`）；速度 LADRC 不是“差速不足”的主因，不能先用速度环
  增益掩盖路径问题。
- 灰度原本已是分段非线性，但在 X7/X8 被 `1600 cps` 饱和。本轮把灰度/总
  差速和丢线保留差速提高至 `2200 cps`；外侧误差增益由 4 提至 5。紧急弯道
  速度保持 280 mm/s。方向符号、JY901 修正上限与 LADRC 参数均不改动。

## 一圈运行通过、A 点自动停车待修（2026-07-30 17:06）

- 镜像：`LineFollowJY901Debug`；冻结证据：
  `line_follow_frozen_20260730-170644.log`。
- 实车观察：小车已连续跑完一圈，B 点不再产生旧 `WIDE` 降速卡顿，首弯和
  后续弯道可通过；用户以 PG10 手动安全停止，故冻结终止原因为
  `PG10_MANUAL_STOP`，不是控制故障。
- 环形冻结窗口覆盖最后 `29.463 s` 连续 `TRACK`，全部 `gyro_fresh=1`；前轮
  目标/实测均值为左 `3233/3198 cps`、右 `1809/1792 cps`。这证明当前 600 mm/s
  基础档、280 mm/s 紧急弯道和 2200 cps 外侧差速能完成一圈，但不构成提速验收。
- 回到 A 点时，弯道末端只短暂出现 `raw=0xFF`，稳定掩码依次为 `0xF0`、`0xF8`
  后离开，未形成当前停车条件要求的连续 `stable_mask=0xFF`。A 点自动停车
  **未通过**；下一阶段应以“弧形 A 标记”的实测掩码序列设计独立终点判定，不能
  继续把全黑直线标记当作唯一条件。
